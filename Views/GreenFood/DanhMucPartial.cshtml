@model IEnumerable<GreenFood.Models.LOAITHUCPHAM>
<div class="sidebar__item">
    <h4>Danh mục</h4>
    <ul class="list-group">
        @foreach (var sp in Model)
        {
            <li><a href="@Url.Action("DanhMuc","GreenFood", new { iMaLoaiTP= sp.MaLoaiThucPham })" >@sp.TenLoaiThucPham</a></li>
        }
    </ul>
</div>

